{
  "name": "reliable-webhook-dispatcher-node",
  "version": "1.0.0",
  "description": "Reliable webhook dispatcher with encrypted payloads, PostgreSQL queue, worker pickup, and retries",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test"
  },
  "keywords": ["node", "express", "webhooks", "postgresql", "sequelize", "queue", "worker", "retries"],
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.3",
    "validator": "^13.15.35"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
